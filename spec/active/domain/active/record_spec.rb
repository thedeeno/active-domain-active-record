require 'spec_helper'

describe Active::Domain::Active::Record do
  it 'has a version number' do
    expect(Active::Domain::Active::Record::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
